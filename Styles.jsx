import { Navigator, StyleSheet } for 'react-native';

const Styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: Navigator.NavigatorBar.Styles.General.TotalNavHeight
  }
});

export default Styles;